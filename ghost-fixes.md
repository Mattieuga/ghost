# Fixes

- confirm override name in new folder

- highlight whole folder in sidebar, should be able to drop on the files inside a folder to highlight that folder and drop there.

- make dragged item smaller

- keep focus on dragged item after drop (expand folder if needed, highlight and select the file)

- always show all folders in side bar, even if they dont contain md files

- silent error when draggin and duplicate name

## **Sidebar**

- collapsing a folder that has the active item in it hides the highlighted row of the menu. If those cases, let’s keep highlighting the folder the item is in. If that folder keeps

- Allow dragging a folder from finder onto the side bar should open that folder as a project

- Add a confirm before deleting a folder just like there is for file. On both make enter confirm the delete instead of cancelling the action.

- Using the right click menu to add a file or folder inside a collapsed folder should expand the folder

- right click in white space allows to add folder - maybe add that everywhere.

## **Editor fixes**

- Too muhc new paragraph spaceaaaa

- Too much space between bullets

- Different spacing between level 2 bullet and the following level 1 bullet

- Collapse section?

- Move section?

## Features

- drag and drop folder to add or in the whitespace of the side bar to open. If drop file in white sapce, open the enclosing folder as the project.
- Multi window - you can double click an md file, it opens in this app without side bar showing, can have many open
- Tabs?
- Search / cmd-k
- Sidebar collapse to just the dots, the dots only show up on hover near the left side. In small width, keep a small border on both side and the dots show up there.
- Outline minimap on right side, hide and only show on hover nearby. hide fully if width is small enough
- Tool bar floating at bottom, toggle somewhere
- light mode
- header title: look at bear - should be a transprent bar at top of editor that will

## Future

- Local and cloud files
- Let's work on the new window feature.
- test
- Asdasdasda