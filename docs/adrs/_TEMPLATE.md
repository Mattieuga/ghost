# <decision title>

> **Filename:** `YYYY-MM-DD-slug.md` — date-prefixed, not numbered (see the naming convention in [`README.md`](./README.md)). Reference this ADR by its slug, not a number.

- **Status:** proposed | accepted | superseded by [the <slug> ADR](./YYYY-MM-DD-slug.md)
- **Decided:** YYYY-MM-DD
- **Supersedes:** <the prior assumption or ADR this overturns, with a link — or "nothing">
- **Reference implementation:** <branch / PR / commit where this first shipped> *(optional)*
- **Related:** <discovery doc this came from · reference doc it updates · sibling ADRs (by slug) · the plan that built it · learnings/runbook around it>

---

## Context

<The forces in play. What problem or pressure forced a decision, and what constraints bound the solution space. Write so someone with zero prior context understands *why a decision was even needed*. Don't describe the solution here.>

## Decision

<What we committed to. If it's several linked sub-decisions, number them `### 1. <name>` … and keep each tight: state the rule, then a line or two of "because Y". This is the load-bearing section — be precise. Prefer "we decided X because Y" over "X is the way.">

## Rejected alternatives

<The options we seriously considered and did not take, each with the one-line reason it lost. This is what stops the decision being re-litigated in six months. If you rejected nothing, you probably decided nothing.>

## Consequences

<What this commits us to — the trade-offs we now own, what gets harder, the follow-on work it implies. Honest, not triumphant.>

## How to apply (the recipe)

<The "what to do" that follows from the decision: the concrete pattern, folder shape, naming, quick-start commands, and a short "what NOT to do" list. This half is what makes an ADR actionable rather than merely historical. Omit only if the decision implies no repeatable procedure.>

## References

<Links: the discovery origin, the reference doc(s) this updated, relevant code, external docs.>
