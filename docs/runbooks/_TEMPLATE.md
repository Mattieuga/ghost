# <system> runbook

The when-it's-on-fire reference. Procedures only — rationale lives in [the relevant ADR](../adrs/YYYY-MM-DD-slug.md).

- **Scope:** <what system / environment / project this covers>
- **Audience:** <who has the access to run these>

---

## <Preconditions / how to open a session> *(if the system needs a special access mode)*

<Setup every procedure below assumes — break-glass shell, active config, env vars, etc.>

```bash
# pastable prelude
```

## <Procedure: do X>

<Short imperative steps. Every command pastable. Pair any UI/console query with its CLI sibling so the procedure is scriptable and agent-drivable.>

```bash
# pastable commands
```

## Common failure modes and how to diagnose

### <symptom>

<What it means · the command to confirm it · the fix.>
